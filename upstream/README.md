# JIZURA upstream snapshot

This directory contains the browser engine and UI files used by the AviUtl2 plugin, copied from [852wa/JIZURA](https://github.com/852wa/JIZURA) at commit `bae339e450512435b829a717248ea92ae5b44899` (2026-09-25, v0.9.0).

The copied files are `src/*.js`, `app/body.html`, `app/style.css`, `vendor/*`, `ae/data.json`, `VERSION`, `LICENSE`, and `THIRD_PARTY_NOTICES.md`. The AviUtl2-specific changes are applied by `build.ps1`; the upstream files remain unchanged. See `LICENSE` for the MIT license and `THIRD_PARTY_NOTICES.md` for bundled dependencies.
